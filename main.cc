#include <iostream>

using namespace std;

int main() {
	
	// for starting new game loop
	while(true) {
		
		for (int i = 0; i < 3; ++i) {
			I made a change
			
		}
		
		for (int i = 3; i >=0; --i) {
			
			
		}
		

		TESTINGTESTINGS
		
		
		// if user doesn't want to start a new game
		// if (....) {
		// 	exit;
		// }
	}

}